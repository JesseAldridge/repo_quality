ssh repo_quality

sudo rm -rf repo_quality.older
sudo cp -r repo_quality.old repo_quality.older

cd repo_quality
git pull

sudo killall -r python
sudo nohup python _3_update_cache_loop.py &

sudo killall -r gunicorn
sudo nohup gunicorn -w 4 _3_serve:app -b 0.0.0.0:80 --log-file=stderr.log --enable-stdio-inheritance &

# output written to: sudo tail nohup.out

# additional debugging
ps aux | grep python
history
sudo killall python
sudo killall gunicorn
df -h

# to test an individual repo:

# data written to: ~/.github_quality
